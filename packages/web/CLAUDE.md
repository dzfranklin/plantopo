Use shadcn instead of reimplementing simple components such as input elements

Use `@remixicon/react` for icons

Use `cn` from `@/lib/utils` for merging Tailwind class names and to organize
long Tailwind class names and split them into multiple lines.
